/*
 * Reto #28
 * MÁQUINA EXPENDEDORA
 * Dificultad: MEDIA
 *
 * Enunciado: Simula el funcionamiento de una máquina expendedora creando una operación
 * que reciba dinero (array de monedas) y un número que indique la selección del producto.
 * - El programa retornará el nombre del producto y un array con el dinero de vuelta (con el menor
 *   número de monedas).
 * - Si el dinero es insuficiente o el número de producto no existe, deberá indicarse con un mensaje
 *   y retornar todas las monedas.
 * - Si no hay dinero de vuelta, el array se retornará vacío.
 * - Para que resulte más simple, trabajaremos en céntimos con monedas de 5, 10, 50, 100 y 200.
 * - Debemos controlar que las monedas enviadas estén dentro de las soportadas.
 */

/*
 * Challenge #28
 * VENDING MACHINE
 * Difficulty: MEDIUM
 *
 * Statement: Simulate the operation of a vending machine by creating an operation
 * that receives money (array of coins) and a number that indicates the selection of the product.
 * - The program will return the name of the product and an array with the money returned (with the smallest
 * number of coins).
 * - If the money is insufficient or the product number does not exist, it must be indicated with a message
 * and return all coins.
 * - If there is no money back, the array will be returned empty.
 * - For simplicity, we will work in cents with 5, 10, 50, 100 and 200 coins.
 * - We must control that the currencies sent are within those supported.
  */